---
// BlogStyles.astro: Diseño profesional y fondo blindado
---
<style is:global>
  :root {
    --cyan: #00e5ff;
    --glass-bg: rgba(10, 25, 41, 0.85);
    --border-glass: rgba(0, 229, 255, 0.2);
  }

  /* BLINDAJE DE FONDO (Evita el blanco al scrollear) */
  html {
    background-color: #030a12 !important;
  }

  body {
    background: radial-gradient(circle at top right, #0a1e30, #030a12 80%) !important;
    background-attachment: fixed !important;
    margin: 0;
    font-family: 'Inter', system-ui, -apple-system, sans-serif;
    color: #f0faff;
    min-height: 100vh;
  }

  header {
    padding: 4rem 1rem 2rem;
    text-align: center;
  }

  header h1 {
    font-size: clamp(2.5rem, 8vw, 4rem);
    margin: 0;
    color: #fff;
    filter: drop-shadow(0 0 10px rgba(0, 229, 255, 0.3));
  }

  /* CONTENEDOR MAESTRO */
  .main-wrapper {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem 5rem;
    display: grid;
    grid-template-columns: 1fr;
    gap: 2.5rem;
  }

  /* INDEX: Dos columnas simétricas (Mac friendly) */
  .home-page .main-wrapper {
    @media (min-width: 950px) {
      grid-template-columns: 1fr 1fr;
      align-items: start;
    }
  }

  /* ENTRADAS: Rejilla de tarjetas */
  .entradas-page .main-wrapper {
    @media (min-width: 768px) {
      grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    }
  }

  /* VIÑETAS (GLASS CARDS) */
  .glass-card {
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--border-glass);
    border-radius: 24px;
    padding: 2.5rem;
    box-shadow: 0 20px 50px rgba(0, 0, 0, 0.5);
    display: flex;
    flex-direction: column;
  }

  .section-label {
    color: var(--cyan);
    font-size: 0.8rem;
    font-weight: 800;
    letter-spacing: 2px;
    text-transform: uppercase;
    margin-bottom: 2rem;
  }

  /* FORMULARIO */
  .styled-form {
    display: flex;
    flex-direction: column;
    gap: 1.2rem;
  }

  .styled-form input, .styled-form textarea {
    width: 100%;
    background: rgba(0, 0, 0, 0.4);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 12px;
    padding: 1.2rem;
    color: #fff;
    font-size: 1rem;
    box-sizing: border-box;
    outline: none;
  }

  .styled-form input:focus, .styled-form textarea:focus {
    border-color: var(--cyan);
  }

  .btn-publish {
    background: var(--cyan);
    color: #010810;
    font-weight: 900;
    padding: 1.2rem;
    border: none;
    border-radius: 12px;
    cursor: pointer;
    text-transform: uppercase;
    transition: transform 0.2s;
  }

  .btn-publish:hover { transform: scale(1.02); }

  /* FEED RECIENTES */
  .feed-item {
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    padding: 1.5rem 0;
  }
  .feed-item:last-child { border-bottom: none; }
  .feed-item h3 { margin: 0; font-size: 1.2rem; color: #fff; }
  .feed-item p { margin: 0.5rem 0 0; color: rgba(255,255,255,0.6); font-size: 0.95rem; }
  .time-tag { color: var(--cyan); font-size: 0.75rem; font-weight: 600; }

  /* BOTÓN VER TODAS */
  .btn-view-all {
    color: #fff;
    text-decoration: none;
    font-size: 0.75rem;
    font-weight: 700;
    padding: 0.6rem 1.2rem;
    border: 1px solid rgba(255,255,255,0.2);
    border-radius: 10px;
  }
  .btn-view-all:hover { background: #fff; color: #000; }

  /* BOTÓN ELIMINAR */
  .btn-delete {
    background: rgba(255, 107, 107, 0.1);
    color: #ff6b6b;
    border: 1px solid rgba(255, 107, 107, 0.2);
    padding: 0.6rem;
    border-radius: 8px;
    cursor: pointer;
    margin-top: 1.5rem;
  }
</style>