---
// BlogStyles.astro: Solución definitiva al fondo blanco y estilos base
---
<style is:global>
  :root {
    --primary-cyan: #00e5ff;
    --glass-bg: rgba(10, 25, 41, 0.9);
    --border-glass: rgba(0, 229, 255, 0.2);
  }

  /* 1. BLINDAJE CONTRA FONDO BLANCO */
  /* Aplicamos el color oscuro al HTML para que el "vaciado" del scroll sea negro */
  html {
    background-color: #030a12 !important;
    margin: 0;
    padding: 0;
    height: 100%;
  }

  body {
    /* El degradado ahora está anclado (fixed). No se desplaza con el scroll */
    background: radial-gradient(circle at top right, #0a1e30, #030a12 80%) !important;
    background-attachment: fixed !important;
    background-repeat: no-repeat !important;
    background-size: cover !important;
    
    margin: 0;
    font-family: 'Inter', system-ui, -apple-system, sans-serif;
    color: #f0faff;
    min-height: 100vh;
    line-height: 1.6;
  }

  /* 2. ESTILOS DE CABECERA (Se mantienen coherentes) */
  header {
    padding: 4rem 1rem 2rem;
    text-align: center;
  }

  header h1 {
    font-size: clamp(2.5rem, 8vw, 4rem);
    margin: 0;
    color: #fff;
    filter: drop-shadow(0 0 15px rgba(0, 229, 255, 0.2));
  }

  .date-badge {
    color: var(--primary-cyan);
    font-size: 0.9rem;
    font-weight: 600;
    letter-spacing: 2px;
    text-transform: uppercase;
    margin-top: 0.5rem;
  }

  /* 3. CONTENEDOR DE CENTRADO (Preparado para los siguientes pasos) */
  main {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 1.5rem 5rem;
  }

  /* Clases de utilidad para las viñetas que usaremos luego */
  .glass-card {
    background: var(--glass-bg);
    backdrop-filter: blur(20px);
    border: 1px solid var(--border-glass);
    border-radius: 24px;
    padding: 2.5rem;
    box-shadow: 0 20px 50px rgba(0, 0, 0, 0.5);
  }

  .muted {
    color: #8cb9c8;
    opacity: 0.7;
  }
</style>