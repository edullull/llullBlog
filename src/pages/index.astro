---
import BlogStyles from '../components/BlogStyles.astro';

const nombre = "Eduardo";
const hoy = new Date().toLocaleDateString('es-ES');
---

<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="icon"
      type="image/svg+xml"
      href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 64 64'><rect fill='%23031420' width='64' height='64'/><circle fill='%237be0ff' cx='32' cy='32' r='18'/><path fill='%23ffffff' d='M25 34l7-12 7 12h-14z'/></svg>"
    />
    <title>Mi Blog Personal | LlullBlog</title>
    <BlogStyles />
  </head>
  <body class="home-page">
    <header>
      <h1>¡Hola {nombre}!</h1>
      <h2>Hoy es: {hoy}</h2>
    </header>

    <main>
      <section class="home-new-entry">
        <h2>Nueva entrada</h2>
        <form id="new-entry-form" class="entry-form">
          <div>
            <label for="entry-title">Título</label>
            <input id="entry-title" name="title" type="text" placeholder="Ej: Mi primer post" required />
          </div>
          <div>
            <label for="entry-body">Contenido</label>
            <textarea
              id="entry-body"
              name="body"
              rows="4"
              placeholder="Escribe aquí tu entrada..."
              required
            ></textarea>
          </div>
          <button type="submit" id="submit-btn">Guardar entrada</button>
          <div id="entry-status" class="entry-status" aria-live="polite"></div>
        </form>
      </section>

      <section>
        <h2>Mis entradas recientes (Nube)</h2>
        <div id="custom-entries-list" class="entry-list" aria-live="polite">
          <p class="muted">Cargando entradas desde Supabase...</p>
        </div>
      </section>

      <section>
        <h2><a href="/entradas">Ver todas las entradas</a></h2>
        <p>Accede al listado completo de publicaciones.</p>
      </section>
    </main>

    <script>
      import { createClient } from '@supabase/supabase-js';
    
      const supabaseUrl = import.meta.env.PUBLIC_SUPABASE_URL;
      const supabaseKey = import.meta.env.PUBLIC_SUPABASE_ANON_KEY;
      const supabase = createClient(supabaseUrl, supabaseKey);
    
      const form = document.getElementById('new-entry-form') as HTMLFormElement;
      const titleEl = document.getElementById('entry-title') as HTMLInputElement;
      const bodyEl = document.getElementById('entry-body') as HTMLTextAreaElement;
      const listEl = document.getElementById('custom-entries-list') as HTMLElement;
      const submitBtn = form.querySelector('button') as HTMLButtonElement;
    
      // --- PUNTO 3: NOTIFICACIONES (TOASTS) ---
      function showToast(message: string, type: 'success' | 'error' = 'success') {
        let container = document.getElementById('toast-container');
        if (!container) {
          container = document.createElement('div');
          container.id = 'toast-container';
          document.body.appendChild(container);
        }
        const toast = document.createElement('div');
        toast.className = `toast ${type}`;
        toast.innerHTML = `<span>${type === 'success' ? '✅' : '❌'}</span><span>${message}</span>`;
        container.appendChild(toast);
        setTimeout(() => toast.remove(), 3000);
      }
    
      // --- PUNTO 2: TIEMPO RELATIVO (A PRUEBA DE VERCEL) ---
      function getRelativeTime(dateInput: any) {
        if (!dateInput) return '';
        const date = new Date(dateInput).getTime();
        const now = new Date().getTime();
        const diffInSeconds = Math.floor((now - date) / 1000);
        const rtf = new Intl.RelativeTimeFormat('es', { numeric: 'auto' });
    
        if (diffInSeconds < 40) return 'hace un momento';
        if (diffInSeconds < 3600) return rtf.format(-Math.floor(diffInSeconds / 60), 'minutes');
        if (diffInSeconds < 86400) return rtf.format(-Math.floor(diffInSeconds / 3600), 'hours');
        if (diffInSeconds < 604800) return rtf.format(-Math.floor(diffInSeconds / 86400), 'days');
        return new Date(dateInput).toLocaleDateString('es-ES', { day: 'numeric', month: 'long' });
      }
    
      // --- PUNTO 4: RENDERIZADO CON ESTADO VACÍO ---
      const renderEntries = async () => {
        listEl.innerHTML = '<p class="muted">Actualizando feed...</p>';
        
        const { data: entries, error } = await supabase
          .from('entradas')
          .select('*')
          .order('created_at', { ascending: false })
          .limit(5);
    
        if (error) {
          listEl.innerHTML = '<div class="empty-state"><p>Error al conectar con la base de datos</p></div>';
          return;
        }
    
        // SI NO HAY ENTRADAS (ESTADO VACÍO)
        if (!entries || entries.length === 0) {
          listEl.innerHTML = `
            <div class="empty-state">
              <div class="empty-icon">✍️</div>
              <p>Tu diario está vacío</p>
              <small>Escribe tu primera historia en el formulario de arriba.</small>
            </div>
          `;
          return;
        }
    
        listEl.innerHTML = '';
        entries.forEach((entry) => {
          const card = document.createElement('article');
          card.className = 'entry-card';
          card.innerHTML = `
            <h3>${entry.title}</h3>
            <p>${entry.body}</p>
            <p class="muted"><small>🕒 ${getRelativeTime(entry.created_at)}</small></p>
          `;
          listEl.appendChild(card);
        });
      };
    
      // --- PUNTO 1: ENVÍO CON ESTADO DE CARGA ---
      form.addEventListener('submit', async (e) => {
        e.preventDefault();
        const title = titleEl.value.trim();
        const body = bodyEl.value.trim();
    
        if (!title || !body) {
          showToast('Por favor, rellena todos los campos', 'error');
          return;
        }
    
        submitBtn.disabled = true;
        const originalBtnText = submitBtn.innerText;
        submitBtn.innerText = '🚀 Publicando...';
    
        const { error } = await supabase.from('entradas').insert([{ title, body }]);
    
        if (error) {
          showToast('Error: ' + error.message, 'error');
        } else {
          showToast('¡Entrada publicada con éxito!');
          form.reset();
          await renderEntries();
        }
    
        submitBtn.disabled = false;
        submitBtn.innerText = originalBtnText;
      });
    
      renderEntries();
    </script>
  </body>
</html>
